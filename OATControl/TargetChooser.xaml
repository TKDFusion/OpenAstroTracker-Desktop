<controls:ThemedWindow x:Class="OATControl.TargetChooser"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:OATControl.Controls"
        xmlns:local="clr-namespace:OATControl"
        xmlns:converters="clr-namespace:OATControl.Converters"
   		controls:ThemedWindow.TitleBarButtons="Minimize,Close" 
        mc:Ignorable="d"
		Background="{DynamicResource AppWindowBackgroundBrush}"
        Title="Target Chooser" Height="600" MinHeight="280" Width="550" MinWidth="210" MaxWidth="580">
	<Window.Resources>
		<converters:DoubleToHMSConverter x:Key="RAConverter" />
		<converters:DoubleToHMSConverter x:Key="DECConverter" Formatter = "[0:+00;-00]° [1:00]&quot; [2:00]'"/>
		<converters:DoubleToHMSConverter x:Key="DECNoSignConverter" Formatter = "[0:00]° [1:00]&quot; [2:00]'"/>

		<converters:DoubleToGridLengthDividerConverter x:Key="RADiffConverter" Divisor= "1"/>
		<converters:DoubleToGridLengthDividerConverter x:Key="RADiffInvertConverter" Divisor= "1" Inverter="100"/>
		<converters:DoubleToGridLengthDividerConverter x:Key="DECDiffConverter" Divisor= "1"/>
		<converters:DoubleToGridLengthDividerConverter x:Key="DECDiffInvertConverter" Divisor= "1" Inverter="100"/>
		<converters:DoubleToGridLengthDividerConverter x:Key="DistDiffConverter" Divisor= "1"/>
		<converters:DoubleToGridLengthDividerConverter x:Key="DistDiffInvertConverter" Divisor= "1" Inverter="100"/>
		<converters:BoolToVisibilityConverter x:Key="BoolToVisibleConverter"  Collapse="True" />
		<converters:BoolToVisibilityConverter x:Key="BoolToHiddenConverter"  IsReversed="True" />

		<Style x:Key="HeaderStyle" TargetType="TextBlock" >
			<Setter Property="FontSize" Value="13" />
			<Setter Property="FontWeight" Value="Bold" />
			<Setter Property="HorizontalAlignment" Value="Left" />
			<Setter Property="Foreground" Value="{DynamicResource AppForegroundStrongBrush}" />
			<Setter Property="Padding" Value="10,4" />
			<Setter Property="HorizontalAlignment" Value="Stretch" />
			<Setter Property="Background" Value="{DynamicResource AppButtonBackgroundBrush}" />
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="true">
					<Setter Property="Background" Value="{DynamicResource AppForegroundBrush}" />
				</Trigger>
			</Style.Triggers>
		</Style>
		<DataTemplate x:Key="myHeaderTemplate">
			<TextBlock Style="{StaticResource HeaderStyle}" >
				<TextBlock.Text>
					<Binding/>
				</TextBlock.Text>
			</TextBlock>
		</DataTemplate>

		<DataTemplate x:Key="RaCellTemplate">
			<TextBlock Padding="10,0" >
				<TextBlock.Text>
					<Binding Path="RA" Converter="{StaticResource RAConverter}"/>
				</TextBlock.Text>
			</TextBlock>
		</DataTemplate>

		<DataTemplate x:Key="NameCellTemplate">
			<TextBlock Padding="10,0" >
				<TextBlock.Text>
					<Binding Path="Name" />
				</TextBlock.Text>
			</TextBlock>
		</DataTemplate>

		<DataTemplate x:Key="CatalogNameCellTemplate">
			<TextBlock Padding="10,0" Width="90">
				<TextBlock.Text>
					<Binding Path="CatalogName" />
				</TextBlock.Text>
			</TextBlock>
		</DataTemplate>
		
		<DataTemplate x:Key="RaDistCellTemplate">
			<Grid>
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="{Binding RAStepDistance, Converter={StaticResource RADiffConverter}}" />
						<ColumnDefinition Width="{Binding RAStepDistance, Converter={StaticResource RADiffInvertConverter}}" />
					</Grid.ColumnDefinitions>

					<Border Grid.Column="0"  >
						<Rectangle Fill="{DynamicResource AppButtonBackgroundBrush}"/>
					</Border>
					<Border Grid.Column="1"  >
						<Rectangle Fill="{DynamicResource AppButtonHoverBrush}"/>
					</Border>
				</Grid>
				<TextBlock Foreground="{DynamicResource AppForegroundSubtleBrush}" Padding="10,0" HorizontalAlignment="Stretch" >
					<TextBlock.Text>
						<Binding Path="RADistance" Converter="{StaticResource RAConverter}"/>
					</TextBlock.Text>
				</TextBlock>
			</Grid>
		</DataTemplate>

		<DataTemplate x:Key="DecCellTemplate">
			<TextBlock Padding="10,0" >
				<TextBlock.Text>
					<Binding Path="DEC" Converter="{StaticResource DECConverter}"/>
				</TextBlock.Text>
			</TextBlock>
		</DataTemplate>

		<DataTemplate x:Key="DecDistCellTemplate">
			<Grid>
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="{Binding DECStepDistance, Converter={StaticResource DECDiffConverter}}" />
						<ColumnDefinition Width="{Binding DECStepDistance, Converter={StaticResource DECDiffInvertConverter}}" />
					</Grid.ColumnDefinitions>

					<Border Grid.Column="0"  >
						<Rectangle Fill="{DynamicResource AppButtonBackgroundBrush}"/>
					</Border>
					<Border Grid.Column="1"  >
						<Rectangle Fill="{DynamicResource AppButtonHoverBrush}"/>
					</Border>
				</Grid>
				<TextBlock Foreground="{DynamicResource AppForegroundSubtleBrush}" Padding="10,0" HorizontalAlignment="Stretch" >
					<TextBlock.Text>
						<Binding Path="DECDistance" Converter="{StaticResource DECNoSignConverter}"/>
					</TextBlock.Text>
				</TextBlock>
			</Grid>
		</DataTemplate>

		<DataTemplate x:Key="DecPosCellTemplate">
			<Grid>
				<TextBlock Foreground="{DynamicResource AppForegroundSubtleBrush}" Padding="10,0" HorizontalAlignment="Stretch" >
					<TextBlock.Text>
						<Binding Path="DECPosition" />
					</TextBlock.Text>
				</TextBlock>
			</Grid>
		</DataTemplate>

		<DataTemplate x:Key="RaPosCellTemplate">
			<Grid>
				<TextBlock Foreground="{DynamicResource AppForegroundSubtleBrush}" Padding="10,0" HorizontalAlignment="Stretch" >
					<TextBlock.Text>
						<Binding Path="RAPosition" />
					</TextBlock.Text>
				</TextBlock>
			</Grid>
		</DataTemplate>

		<DataTemplate x:Key="DistCellTemplate">
			<Border BorderBrush="{DynamicResource AppForegroundBrush}" BorderThickness="1" >
				<Grid>
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="{Binding DistanceNormalized, Converter={StaticResource DistDiffConverter}}" />
							<ColumnDefinition Width="{Binding DistanceNormalized, Converter={StaticResource DistDiffInvertConverter}}" />
						</Grid.ColumnDefinitions>

						<Border Grid.Column="0"  >
							<Rectangle Fill="{DynamicResource AppButtonHoverBrush}"/>
						</Border>
						<Border Grid.Column="1"  >
							<Rectangle Fill="{DynamicResource AppWindowBackgroundBrush}"/>
						</Border>
					</Grid>
					<TextBlock Foreground="{DynamicResource AppForegroundStrongBrush}" Padding="10,0" HorizontalAlignment="Center" FontWeight="Bold">
						<TextBlock.Text>
							<Binding Path="DistanceNormalized" StringFormat="{}{0:0}"/>
						</TextBlock.Text>
					</TextBlock>
				</Grid>
			</Border>
		</DataTemplate>
		<Style TargetType="ListViewItem">
			<Setter Property="HorizontalContentAlignment" Value="Stretch"/>
			<Setter Property="Margin" Value="11,0,1,0"/>
			<Setter Property="Padding" Value="0,2"/>
			<Setter Property="Foreground" Value="{DynamicResource AppForegroundBrush}"/>
			<Setter Property="FontSize" Value="12"/>
			<EventSetter Event='MouseDoubleClick' Handler='HandleDoubleClick'/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type ListViewItem}">
						<Border Name="Border" BorderBrush="{TemplateBinding BorderBrush}" 
                                  BorderThickness="{TemplateBinding BorderThickness}" 
                                  Background="{TemplateBinding Background}">
							<GridViewRowPresenter Content="{TemplateBinding Content}"
                                      Margin="{TemplateBinding Padding}" />
						</Border>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
			<Style.Triggers>
				<Trigger Property="IsSelected" Value="true">
					<Setter Property="Background" Value="{DynamicResource AppButtonHoverBrush}" />
					<Setter Property="Foreground" Value="{DynamicResource AppForegroundStrongBrush}" />
					<Setter Property="BorderBrush" Value="{DynamicResource AppButtonHoverBrush}" />
					<Setter Property="BorderThickness" Value="1" />
				</Trigger>
				<Trigger Property="IsMouseOver" Value="true">
					<Setter Property="Background" Value="{DynamicResource AppButtonHoverBrush}" />
					<Setter Property="BorderBrush" Value="{DynamicResource AppForegroundBrush}" />
					<Setter Property="BorderThickness" Value="1" />
				</Trigger>
				<DataTrigger Binding="{Binding IsReachable}" Value="false">
					<Setter Property="Background" Value="{DynamicResource AppInputBackgroundBrush}" />
					<Setter Property="BorderBrush" Value="{DynamicResource AppBorderSubtleBrush}" />
					<Setter Property="Foreground" Value="{DynamicResource AppForegroundBrush}"/>
				</DataTrigger>
				<MultiDataTrigger >
					<MultiDataTrigger.Conditions>
						<Condition Binding="{Binding ElementName=ShowReachable, Path=IsChecked}" Value="True" />
						<Condition Binding="{Binding IsReachable}" Value="False" />
					</MultiDataTrigger.Conditions>
					<Setter Property="Visibility" Value="Collapsed" />
				</MultiDataTrigger>
			</Style.Triggers>
		</Style>

		<Style x:Key="GridViewColumnHeaderStyle1" TargetType="{x:Type GridViewColumnHeader}">
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
						<Border BorderThickness="0,0,0,1" BorderBrush="{DynamicResource AppBorderBrush}" Background="Transparent">
							<TextBlock x:Name="ContentHeader" Text="{TemplateBinding Content}" Padding="5,5,5,0" Width="{TemplateBinding Width}" TextAlignment="Center" />
						</Border>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
			<Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}"/>
			<Setter Property="Background" Value="{DynamicResource AppButtonBackgroundBrush}"/>
			<Setter Property="OverridesDefaultStyle" Value="True" />
			<Setter Property="Foreground" Value="{DynamicResource AppForegroundStrongBrush}" />
			<Setter Property="FontFamily" Value="Segoe UI" />
			<Setter Property="FontSize" Value="14" />
			<Setter Property="FontWeight" Value="Bold" />
		</Style>

	</Window.Resources>

	<Grid Margin="0,8,0,2">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8">
			<controls:ToggleSwitch
			x:Name="ShowReachable"
			Grid.Row="0"
			Margin="10,0"
			HorizontalAlignment="Center"
			/>
			<TextBlock Text="Showing Unreachable Targets" Style="{DynamicResource MetroTextBlock}" Margin="0,2"/>
		</StackPanel>
		<Border Grid.Row="1"  BorderThickness="1" BorderBrush="{DynamicResource AppButtonBorderBrush}" Margin="16,8">
			<ListView ItemsSource="{Binding AvailablePointsOfInterest}" HorizontalAlignment="Center" Background="{DynamicResource AppNestedBackgroundBrush}" BorderThickness="0">
				<ListView.View>
					<GridView  ColumnHeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle1}" >
						<GridViewColumn Header="Catalog" Width="80" CellTemplate="{StaticResource CatalogNameCellTemplate}" HeaderTemplate="{StaticResource myHeaderTemplate}"/>
						<GridViewColumn Header="Name" CellTemplate="{StaticResource NameCellTemplate}" HeaderTemplate="{StaticResource myHeaderTemplate}"/>
						<GridViewColumn Header="RA" CellTemplate="{StaticResource RaCellTemplate}"/>
						<GridViewColumn Header="DEC"  CellTemplate="{StaticResource DecCellTemplate}"/>
						<GridViewColumn Header="Distance" Width="90" CellTemplate="{StaticResource DistCellTemplate}" />
					</GridView>
				</ListView.View>
			</ListView>
		</Border>
		<TextBlock Grid.Row="2" Text="This list can be edited in the application Settings dialog" HorizontalAlignment="Center" Margin="0,6" Foreground="{DynamicResource AppForegroundBrush}" />
	</Grid>
</controls:ThemedWindow>
