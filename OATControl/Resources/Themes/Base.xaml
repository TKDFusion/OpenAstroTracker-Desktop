<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    xmlns:controls="clr-namespace:OATControl.Controls">

    <!-- Default Font -->
    <FontFamily x:Key="AppFontFamily">Segoe UI</FontFamily>
	<sys:Double x:Key="AppFontSizeXSmall">8</sys:Double>
	<sys:Double x:Key="AppFontSizeSmall">10</sys:Double>
	<sys:Double x:Key="AppFontSize">12</sys:Double>
	<sys:Double x:Key="AppFontSizeLarge">15</sys:Double>
	<sys:Double x:Key="AppFontSizeXLarge">18</sys:Double>
	
	<!-- TextBlock styles -->
    <Style x:Key="MetroTextBlock" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource AppFontSize}" />
    </Style>

    <Style TargetType="TextBlock" BasedOn="{StaticResource MetroTextBlock}" />

    <!-- TextBox styles -->
    <Style x:Key="MetroTextBox" TargetType="TextBox">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource AppFontSize}" />
        <Setter Property="Foreground" Value="{DynamicResource AppForegroundBrush}" />
        <Setter Property="Background" Value="{DynamicResource AppInputBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4,2" />
        <Setter Property="CaretBrush" Value="{DynamicResource AppForegroundBrush}" />
        <Setter Property="SelectionBrush" Value="{DynamicResource AppForegroundBrush}" />
        <Setter Property="SelectionOpacity" Value="0.3" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Focusable="False"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Hidden" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AppButtonBorderBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
							<Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AppButtonBorderBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource AppDisabledBackgroundBrush}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AppDisabledBorderBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource AppDisabledForegroundBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TextBox" BasedOn="{StaticResource MetroTextBox}" />

    <!-- ToolTip styles -->
    <Style x:Key="MetroToolTip" TargetType="ToolTip">
		<Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}" />
		<Setter Property="Background" Value="{DynamicResource AppTooltipBackgroundBrush}" />
		<Setter Property="Foreground" Value="{DynamicResource AppTooltipForegroundBrush}" />
        <Setter Property="Padding" Value="5,2,5,7" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
    </Style>

    <Style TargetType="ToolTip" BasedOn="{StaticResource MetroToolTip}" />

    <!-- Button styles -->
    <Style x:Key="MainButtonStyle" TargetType="Button">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource AppFontSize}" />
        <Setter Property="Foreground" Value="{DynamicResource AppIdealForegroundBrush}" />
        <Setter Property="Background" Value="{DynamicResource AppButtonBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AppButtonBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="10,5" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="2">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AppButtonHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AppButtonPressedBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="{DynamicResource AppDisabledBackgroundBrush}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource AppDisabledBorderBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource AppDisabledForegroundBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Button" BasedOn="{StaticResource MainButtonStyle}" />

    <Style x:Key="AccentedDialogSquareButton" TargetType="Button" BasedOn="{StaticResource MainButtonStyle}" />

    <!-- ToggleButton styles -->
    <Style x:Key="AccentedToggleSquareButtonStyle" TargetType="ToggleButton">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource AppFontSize}" />
        <Setter Property="Foreground" Value="{DynamicResource AppIdealForegroundBrush}" />
        <Setter Property="Background" Value="{DynamicResource AppPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AppBorderSubtleBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="10,5" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="2">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AppAccentHighlightBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AppAccentActiveBrush}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AppPrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="{DynamicResource AppAccentBackgroundBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource AppDisabledForegroundBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ToggleButton" BasedOn="{StaticResource AccentedToggleSquareButtonStyle}" />

    <!-- CheckBox -->
    <Style TargetType="CheckBox">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource AppFontSize}" />
        <Setter Property="Foreground" Value="{DynamicResource AppForegroundBrush}" />
        <Setter Property="Background" Value="Transparent" />
    </Style>

    <!-- RadioButton -->
    <Style TargetType="RadioButton">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource AppFontSize}" />
        <Setter Property="Foreground" Value="{DynamicResource AppForegroundBrush}" />
    </Style>

    <!-- Label -->
    <Style TargetType="Label">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource AppFontSize}" />
        <Setter Property="Foreground" Value="{DynamicResource AppForegroundBrush}" />
    </Style>

    <!-- ComboBox -->
    <Style TargetType="ComboBox">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource AppFontSize}" />
        <Setter Property="Foreground" Value="{DynamicResource AppForegroundBrush}" />
        <Setter Property="Background" Value="{DynamicResource AppInputBackgroundBrush}" />
    </Style>

    <!-- TabControl -->
    <Style TargetType="TabControl">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="Background" Value="{DynamicResource AppWindowBackgroundBrush}" />
    </Style>

    <!-- TabItem -->
    <Style TargetType="TabItem">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource AppFontSize}" />
        <Setter Property="Foreground" Value="{DynamicResource AppForegroundBrush}" />
    </Style>

    <!-- ScrollBar -->
    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="{DynamicResource AppWindowBackgroundBrush}" />
    </Style>

    <!-- ProgressBar -->
    <Style TargetType="ProgressBar">
        <Setter Property="Foreground" Value="{DynamicResource AppPrimaryBrush}" />
        <Setter Property="Background" Value="{DynamicResource AppAccentBackgroundBrush}" />
    </Style>

    <!-- MenuItem -->
    <Style x:Key="MetroMenuItem" TargetType="MenuItem">
        <Setter Property="Foreground" Value="{DynamicResource AppForegroundBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource AppFontSize}" />
    </Style>

    <!-- ListBoxItem / ListViewItem -->
    <Style x:Key="MetroListBoxItem" TargetType="ListBoxItem">
        <Setter Property="Foreground" Value="{DynamicResource AppForegroundBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="Padding" Value="4,2" />
    </Style>

    <!-- ThemedWindow Style -->
    <Style TargetType="controls:ThemedWindow">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:ThemedWindow">
                    <AdornerDecorator>
                        <Grid Background="{TemplateBinding Background}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!-- Title Bar -->
                            <Border x:Name="TitleBarBorder"
                                    Grid.Row="0"
                                    Height="30"
                                    Background="{DynamicResource AppTitleBarBackgroundBrush}"
                                    BorderBrush="{DynamicResource AppBorderSubtleBrush}"
                                    BorderThickness="0,0,0,1"
                                    WindowChrome.IsHitTestVisibleInChrome="False">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Icon + Title -->
                                    <StackPanel Orientation="Horizontal" Margin="8,0,0,0" VerticalAlignment="Center">
                                        <Image Source="{TemplateBinding Icon}"
                                               Width="24" Height="24"
                                               VerticalAlignment="Center"
                                               RenderOptions.BitmapScalingMode="HighQuality" />
                                        <TextBlock Text="{TemplateBinding Title}"
                                                   Foreground="{DynamicResource AppTitleBarForegroundBrush}"
                                                   FontSize="12"
                                                   VerticalAlignment="Center"
                                                   Margin="6,0,0,0"
                                                   TextTrimming="CharacterEllipsis" />
                                    </StackPanel>

                                    <!-- Minimize Button -->
                                    <Button x:Name="MinimizeButton"
                                            Grid.Column="1"
                                            Width="46" Height="30"
                                            WindowChrome.IsHitTestVisibleInChrome="True"
                                            Command="SystemCommands.MinimizeWindowCommand"
                                            Cursor="Hand">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="Bd" Background="Transparent">
                                                    <Path Data="M16,0 L32,0 32,2 16,2z"
                                                          Stretch="Uniform" Width="10" Height="1"
                                                          Fill="{DynamicResource AppTitleBarForegroundBrush}"
                                                          HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AppTitleBarButtonHoverBrush}" />
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AppTitleBarButtonPressedBrush}" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>

                                    <!-- Maximize Button -->
                                    <Button x:Name="MaximizeButton"
                                            Grid.Column="2"
                                            Width="46" Height="30"
                                            WindowChrome.IsHitTestVisibleInChrome="True"
                                            Command="SystemCommands.MaximizeWindowCommand"
                                            Cursor="Hand">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="Bd" Background="Transparent">
                                                    <Path Data="M0,0 L12,0 12,12 0,12z M1,1 L1,11 11,11 11,1z"
                                                          Stretch="Uniform" Width="10" Height="10"
                                                          Fill="{DynamicResource AppTitleBarForegroundBrush}"
                                                          HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AppTitleBarButtonHoverBrush}" />
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AppTitleBarButtonPressedBrush}" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>

                                    <!-- Restore Button -->
                                    <Button x:Name="RestoreButton"
                                            Grid.Column="2"
                                            Width="46" Height="30"
                                            WindowChrome.IsHitTestVisibleInChrome="True"
                                            Command="SystemCommands.RestoreWindowCommand"
                                            Cursor="Hand"
                                            Visibility="Collapsed">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="Bd" Background="Transparent">
                                                    <Path Data="M2,0 L10,0 10,2 12,2 12,10 10,10 10,12 0,12 0,4 2,4z M2,2 L2,4 10,4 10,10 12,10 12,2z M0,4 L8,4 8,12 0,12z"
                                                          Stretch="Uniform" Width="10" Height="10"
                                                          Fill="{DynamicResource AppTitleBarForegroundBrush}"
                                                          HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AppTitleBarButtonHoverBrush}" />
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AppTitleBarButtonPressedBrush}" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>

                                    <!-- Close Button -->
                                    <Button x:Name="CloseButton"
                                            Grid.Column="3"
                                            Width="46" Height="30"
                                            WindowChrome.IsHitTestVisibleInChrome="True"
                                            Command="SystemCommands.CloseWindowCommand"
                                            Cursor="Hand">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="Bd" Background="Transparent">
                                                    <Path Data="M0,0 L2,0 8,6 14,0 16,0 16,2 10,8 16,14 16,16 14,16 8,10 2,16 0,16 0,14 6,8 0,2z"
                                                          Stretch="Uniform" Width="10" Height="10"
                                                          Fill="{DynamicResource AppTitleBarForegroundBrush}"
                                                          HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Bd" Property="Background" Value="#E81123" />
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="Bd" Property="Background" Value="#F1707A" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                            </Border>

                            <!-- Window Content -->
                            <ContentPresenter Grid.Row="1"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}" />
                        </Grid>
                    </AdornerDecorator>

                    <ControlTemplate.Triggers>
                        <!-- Maximize/Restore toggle -->
                        <Trigger Property="WindowState" Value="Maximized">
                            <Setter TargetName="MaximizeButton" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="RestoreButton" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <!-- Hide title bar when ShowTitleBar=False -->
            <Trigger Property="controls:ThemedWindow.ShowTitleBar" Value="False">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="controls:ThemedWindow">
                            <AdornerDecorator>
                                <ContentPresenter Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}" />
                            </AdornerDecorator>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>